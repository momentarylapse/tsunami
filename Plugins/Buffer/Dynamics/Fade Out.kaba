// Image = hui:zoom-out
// Title[Deutsch] = Ausblenden

use "../../api.kaba"

void ProcessTrack(BufferBox buf)
	int i
	for i, 0, buf.num
		float vol = 1 - i2f(i) / buf.r.num
		buf.r[i] = buf.r[i] * vol
		buf.l[i] = buf.l[i] * vol

