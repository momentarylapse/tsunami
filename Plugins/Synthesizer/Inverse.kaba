use "../api.kaba"


class InverseSynthesizer : Synthesizer
	DummySynthesizer sub
	overwrite void RenderNote(BufferBox buf, Range r, float pitch, float volume)
		sub.sample_rate = sample_rate
		sub.RenderNote(buf, r, 150 - pitch, volume)
