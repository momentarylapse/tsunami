// Title[Deutsch] = Rauschen
use "../../api.kaba"

class Noise extends AudioEffect
	override void process(BufferBox buf)
		for i in 0:buf.length
			buf.r[i] = rand(2.0) - 1.0
			buf.l[i] = rand(2.0) - 1.0

